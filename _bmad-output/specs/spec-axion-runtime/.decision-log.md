# Decision Log — spec-axion-runtime

## Create — 2026-05-26

**Pass 1 — Coherence (Spec Law 1–6, 8):**

- Rule 1 (intent + success): All 10 capabilities have both fields. Pass.
- Rule 2 (WHAT not HOW): All intents describe outcomes, not implementations. Pass.
- Rule 3 (constraints bend design): 7 constraints, each rules something out (Swift-only, append-only, no UI state, <1s restore, etc.). Pass.
- Rule 4 (non-goals explicit): 8 non-goals listed. Pass.
- Rule 5 (success signal concrete): The success signal describes a specific scenario (run, close terminal, resume, observe stream, second user via HTTP). Testable. Pass.
- Rule 6 (capability IDs stable): CAP-1 through CAP-10, no gaps, no reuse. Pass.
- Rule 8 (lean prose): No hedges, backstory, or decoration in SPEC.md. Companions hold detail that would bloat the kernel. Pass.

**Pass 2 — Preservation:**

Walked all six source files claim by claim:

- PRD: All 15 functional requirements (FR1–FR15) landed as capabilities or constraints. Personas and UX principles are wrapper context — not load-bearing for the build contract. Dropped under "Wrapper-only content."
- Architecture: System topology, component design, data flow all preserved in `architecture-diagrams.md`. Runtime-first, event-driven, append-only principles captured as constraints.
- Runtime Model: Event taxonomy (5 categories, ~25 event types) preserved in `event-taxonomy.md`. State machines (4 entities) preserved in `state-machines.md`. Core design principles (event-driven, DAG-based, replayable) in SPEC kernel.
- API Spec: All endpoints and protocols preserved in `api-protocol.md`. Compatibility rule (100% event-stream-based UI) captured as constraint.
- Axion Roadmap: Phased delivery (A1–A7 + SDK S1–S5), dependency graph, transition strategy, and timeline preserved in `implementation-roadmap.md`. Handler mapping table preserved.
- SDK Roadmap: Event layer design (S1–S5), EventBus implementation details, Agent.swift emit points all preserved across companions. Backward compatibility requirement captured as non-goal and constraint.

**Assumptions noted:** 5 assumptions logged (SDK Epic 1 timing, SessionStore correctness, STDIO-first transport, SQLite sufficiency, Phase 1 scope).

**Open questions logged:** 5 questions (EventBus buffer strategy, token streaming cost, agent communication model, sequential vs DAG in Phase 1, error recovery granularity).

**Wrapper-only content (intentionally dropped):**
- PRD personas P1–P3 (descriptive context, no design impact)
- PRD principles §6.1–6.5 (redundant with constraints/capabilities)
- PRD metrics §11 (dashboard metrics, not build requirements)
- PRD long-term vision §12 (already in non-goals or future phases)
- Architecture extension points §8 (future, not load-bearing for Phase 1)
- API spec §14 status table (snapshot, not contract)
- Both roadmaps' risk tables (process metadata, preserved in roadmap companion)

**Verdict:** Spec is coherent and preservation-complete. All load-bearing claims from the six source documents landed in SPEC.md or a companion. 10 capabilities, 7 constraints, 8 non-goals. 5 companions spawned. 5 open questions need human decisions before downstream consumption.
